# Final site snapshot

The last published state of <https://ijd5004.github.io/wc-2026/>, captured from
the `github-pages` artifact of the final scheduled `Update pool` run
([31191350181](https://github.com/ijd5004/wc-2026/actions/runs/31191350181),
2026-08-07 15:11 UTC).

`index.html` is fully self-contained — no external CSS, JS, fonts, or images —
so it renders correctly opened straight from disk. `card.png` is the share card;
it is not referenced by the page.

This is a frozen copy for the record. The generated site is still built into the
gitignored `site/` directory by `python -m wcpool.site`, and the live Pages
deployment continues to serve the same content.

Final standings: Bob wins with 92 points (Spain, 52).
