name: Update pool

on:
  # The tournament is over and the pool is settled, so the hourly schedule that
  # polled football-data.org has been removed — every run since the final only
  # rewrote the `generated_at` timestamp. The job is kept dispatch-only so the
  # site can still be rebuilt by hand from the committed data if needed; run it
  # with skip_fetch to rebuild without touching the API.
  workflow_dispatch:
    inputs:
      skip_fetch:
        description: "Skip the API fetch and rebuild from committed data (no key needed)"
        type: boolean
        default: false

concurrency:
  group: update-pool
  cancel-in-progress: false

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  update:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Install
        run: pip install -e ".[card]"

      - name: Fetch results
        if: ${{ !(github.event_name == 'workflow_dispatch' && inputs.skip_fetch) }}
        env:
          FOOTBALL_DATA_API_KEY: ${{ secrets.FOOTBALL_DATA_API_KEY }}
        run: python -m wcpool.fetch --data-dir data

      - name: Score
        run: python -m wcpool.scoring data/matches.json data/pool.json data/standings.json

      - name: Build site
        run: python -m wcpool.site --data-dir data --out site

      - name: Render share card
        run: python -m wcpool.card --data-dir data --out site/card.png

      - name: Commit data snapshots
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/
          if ! git diff --cached --quiet; then
            git commit -m "Data update $(date -u +%F) [skip ci]"
            git push
          fi

      - name: Upload site artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: site/

      - name: Deploy to GitHub Pages
        id: deploy
        uses: actions/deploy-pages@v5
